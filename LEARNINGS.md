# LEARNINGS

Durable notes for future work:

- sanger-viewer is a client-side TypeScript Sanger trace workbench for bench scientists; preserve local/private trace handling and GitHub Pages `/sanger-viewer/` base-path support.
- Standard validation commands are `npm run lint`, `npm run typecheck`, `npm run test`, `npm run test:e2e`, `npm run perf:smoke`, `npm run build`, and `npm run ux:gallery` for the UX screenshot gate.
- UX-affecting PRs should capture desktop, tablet, and mobile screenshots for key states, then include a written review of what is great and what should improve.
- New durable development decisions belong in `docs/adr/`; reusable repo knowledge belongs here; shipped work should be summarized in the devlog.
